import { motion } from "framer-motion"
import { FaArrowUpRightFromSquare } from "react-icons/fa6"
import projects from "../data/projects"

export default function Projects(){
 const selected=projects.filter(p=>["Controlware VBA Billing System","Tradesman ERP","Controlware Engineering Website","TradesmanBD","Own Portfolio","Tarunyo Hira", "Sunpac Website", "JASRIR Website", "Band Tarunyo", "Al Muneer Trading (Ongoing)"].includes(p.title))
 return <section id="projects" className="py-32 px-10"><div className="max-w-7xl mx-auto"><motion.div initial={{opacity:0,y:50}} whileInView={{opacity:1,y:0}} viewport={{once:true}}><p className="section-kicker">Selected systems & digital work</p><div className="grid lg:grid-cols-[1fr_.7fr] gap-12 items-end"><h2 className="section-title">Things I built because a problem, business or idea needed a shape.</h2><p className="text-gray-400 text-lg leading-relaxed">Not a complete archive. These projects show the range—from internal workflow automation and business systems to websites and creative platforms.</p></div></motion.div><div className="editorial-projects mt-20">{selected.map((p,i)=><motion.article key={p.title} initial={{opacity:0,y:45}} whileInView={{opacity:1,y:0}} viewport={{once:true}} transition={{delay:(i%2)*.08}} className={`editorial-project ${i===0||i===3?'project-wide':''}`}><div className="project-image"><img src={p.image} alt={p.title}/><span>0{i+1}</span></div><div className="project-body"><p className="project-type">{p.tech.join(' / ')}</p><h3>{p.title}</h3><p>{p.description}</p><div className="project-links">{p.live&&p.live!=="#"&&<a href={p.live} target="_blank" rel="noreferrer">View project <FaArrowUpRightFromSquare/></a>}{p.github&&p.github!=="#"&&<a href={p.github} target="_blank" rel="noreferrer">GitHub <FaArrowUpRightFromSquare/></a>}</div></div></motion.article>)}</div></div></section>
}
